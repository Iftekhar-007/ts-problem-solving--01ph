<h1>Blogs</h1>

<h2>Provide an example of using union and intersection types in TypeScript.</h2>

<h3>🔵 TypeScript–এ Union ও Intersection</h3>

<p>TypeScript–এ union আর intersection দুইটাই বেশ ব্যবহার হয়। ডেভেলপারদের কাছে এগুলো খুবই পছন্দের কারণ এগুলো কোডকে আরও শক্তিশালী আর নমনীয় করে।</p>

<h4>🟦 Union টাইপ</h4>

Union মানে হল— দুটি বা তার বেশি জিনিসের মধ্যে যে কোন একটি হতে পারে।

এটিকে চিহ্নিত করা হয় এই চিহ্ন দিয়ে: |

<p><b>examplpe:</b></p>
let value: string | number;

value = "Shawon";
value = 30;

এখানে value কখনো স্ট্রিং, আবার কখনো সংখ্যা— দুটোই হতে পারে।

<h4>🟩 Intersection টাইপ</h4>

Intersection মানে হল— দুইটি টাইপের সব গুণাবলি একসাথে থাকতে হবে।
এটিকে দেখানো হয় & চিহ্ন দিয়ে।

মানে,
“দুটোর সব বৈশিষ্ট্যই লাগবে একসাথে।”

উদাহরণঃ
type User = {
name: string;
};

type Admin = {
role: string;
};

type AdminUser = User & Admin;

const person: AdminUser = {
name: "shawon",
role: "admin",
};

এখানে person–কে User ও Admin—দুটোরই বৈশিষ্ট্য রাখতে হয়েছে।

<!-- ! another -->

<h2>Explain the difference between any, unknown, and never types in TypeScript.</h2>

<h4>🟦 1. any টাইপ</h4>
যেকোনো কিছুই এর ভিতরে ঢুকবে।

➡️ TypeScript একদম চেক করবে না

➡️ যেকোনো অপারেশন করা যাবে

➡️ কিন্তু এর জন্য bug হওয়ার সম্ভাবনা বেশি

<p><b>Example:</b></p>
let data: any;

data = 10;
data = "text";
data = true;

কোনো এরর দিবে না ।

<h4>🟩 2. unknown টাইপ</h4>

any-এর মতন flexible, কিন্তু বেশি নিরাপদ।

যেকোনো মান রাখা যাবে,

কিন্তু মান ব্যবহার করার আগে TypeScript type-check চাইবে

directly function call/operation করা যাবে না ।

উদাহরণ:
let value: unknown;

value = "shawon";

value.toUpperCase(); // ❌ Error দিবে ।

কেন? কারণ TypeScript নিশ্চিত না যে value string কিনা।

ঠিকভাবে ব্যবহার করলে:
if (typeof value === "string") {
value.toUpperCase(); // ✔️ OK
}

সংক্ষেপে:
unknown = “যাই দিবো ঠিক আছে, কিন্তু ব্যবহার করার আগে যাচাই করতে হবে।”

🟥 3. never টাইপ

যে জিনিস কখনোই ঘটবে না।

এই টাইপ সাধারণত তিন জায়গায় পাওয়া যায়:

(1) ফাংশন যেটা কখনো return করে না
function throwError(): never {
throw new Error("Boom!");
}

(2) endless loop
function loopForever(): never {
while (true) {}
}

(3) impossible type
type A = string & number; // never
